kind: review
name: pr-review
# Crosses the PR-scoped dispatch gate: the run lock shared with every other
# PR-scoped workflow, per-head-SHA dedup, escalation, and the resolved
# `PrState` snapshot on `context.prState`. A fork that drops the key loses all
# four.
pr_scoped: true
git_access: review-write
workspace: per-target-reuse
prepopulate_pr_head_ref: true
classification:
  intent: review
  description: |
    REVIEW — The user wants a code review. Either repo-wide ("review cliftonc/repo", "check PRs", "can you review PRs on <repo>?") OR, when the comment is a reply on a PR (ISSUE TITLE present), a request to review THIS PR: "review this", "can you review this?", "please review", "take a look at this PR", "give this a review". The deliverable is a formal PR review (inline comments on the diff). Prefer REVIEW over CHAT whenever there's a clear "review"/"take a look" request on a PR.
  examples:
    - '"please review https://github.com/foo/bar/pull/42" → INTENT: REVIEW, REPO: foo/bar, ISSUE: 42, REASON: NONE'
    - '"can you review this?" with ISSUE TITLE "Add HTML sanitizer for messages" → INTENT: REVIEW, REPO: NONE, ISSUE: NONE, REASON: NONE'
    - '"take a look at this PR when you get a chance" with ISSUE TITLE "Port fastify adapter" → INTENT: REVIEW, REPO: NONE, ISSUE: NONE, REASON: NONE'
chat:
  trigger: review PRs on owner/repo
  summary: Review the open PRs on a repo
  deflect:
    - review PRs on <repo>
    - check open PRs
description: |
  Review a single pull request: read the diff, check for issues, and post one
  formal review. Never modifies code.

phases:
  # HOW MUCH REVIEW IS THIS PUSH OWED? (issue #378) — the new root, and the only
  # phase that runs before the workspace is put to work.
  #
  # A RE-REVIEW only. On a first review there is nothing to compare against, so
  # `reviewIsRereview != true` skips the phase and the full brief runs. Both
  # guards use the BARE-BOOLEAN `!= true` form deliberately: that form runs an
  # absent value through `coerceBool`, so a missing or misspelled context key
  # matches the guard and the phase skips — leaving a full review. The
  # quoted-string form has the opposite behaviour and must not be used here.
  #
  # The phase ends with one `REVIEW_DEPTH: full|light` line, which
  # `engine/review-triage.ts` harvests onto `scratch.reviewTriage` from the same
  # `onPhaseEnd` hook the fix markers ride. On `light`, the seven phases below
  # skip and `prompts/review.md` renders its single-pass arm. On `full`, on no
  # marker, or on a failed phase, the dispatch-time seed stands and the run is
  # exactly what it is today.
  - name: triage
    label: Triage · how much review is owed
    prompt: prompts/review-triage.md
    model: "{{models.review-triage}}"
    skip_if:
      - "triageEnabled != true"
      - "reviewIsRereview != true"
    timeout_seconds: { from: triageTimeoutSeconds }
    # NO `on_output.requires_marker`, deliberately — see AC2 in
    # `tests/workflows/pr-review-probes.test.ts`: nothing in this workflow may
    # declare an `on_output` action, because a red run posts
    # `messages.on_failure`, offers a Retry that cannot succeed, pollutes the
    # cost stats and defeats the SHA dedup. A missing marker already means
    # "full review" — the harvest leaves the dispatch-time seed standing — so
    # failing the run over one would trade a free correct answer for a broken
    # review.

  # Everything from here up to `reconcile` is the evidence pipeline, and all of
  # it is INERT unless `review.analysis.enabled` — each phase carries
  # `skip_if: analysisEnabled != true`, and an absent variable reads as false,
  # so a typo skips the analysis rather than running an unasked-for one.
  # `prepare` and `falsify` need `review.analysis.probes` on top.
  #
  # Each also carries the TIER guard, `scratch.reviewTriage.depth == 'light'`.
  # Quoted `==` there, the mirror image of the bare-boolean form above: an
  # absent value never matches, so the phase RUNS — again failing toward a full
  # review. `review` and `post-review` carry no tier guard and always run.
  - name: prepare
    label: Prepare · probe environment
    type: bash
    # `all_done`, not `all_success`: `triage` skips on a first review and on
    # every deployment that turned it off, and a skipped node is not
    # `succeeded`. A triage phase that FAILED must not take the review with it
    # — the run then owes a full review, which is what the un-replaced seed
    # already says.
    depends_on: [triage]
    trigger_rule: all_done
    skip_if:
      - "analysisEnabled != true"
      - "probesEnabled != true"
      - "scratch.reviewTriage.depth == 'light'"
    timeout_seconds: { from: probePhaseTimeoutSeconds }
    command: |
      set -u
      FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
      OUT=.lastlight/pr-review/probes/env.json
      mkdir -p .lastlight/pr-review/probes

      # `installed` is read off the filesystem even here: a dead process still
      # leaves whatever it unpacked, and that is the only question asked later.
      fallback() {
        if [ -d node_modules ]; then INSTALLED=true; else INSTALLED=false; fi
        printf '{"version":1,"generatedAt":"%s","repo":"{{owner}}/{{repo}}","packageManager":null,"install":"failed","installed":%s,"lifecycleScripts":false,"typecheck":"failed","typecheckDiagnostics":[],"coverage":"failed","coverageReport":null,"durationMs":{"install":0,"typecheck":0,"coverage":0,"total":0},"degraded":[{"extractor":"prepare","reason":"%s"}]}\n' \
          "$(date -u +%Y-%m-%dT%H:%M:%S.000Z)" "$INSTALLED" "$1" > "$OUT"
      }

      ARGS="--repo . --out $OUT"
      # The INSTALL decision, and the only one whose default direction is
      # inverted: anything that is not the literal "true" adds `--no-install`,
      # so `probes: static` — and an absent value — prepares the tree without
      # ever running a package manager. `env.json` then says
      # `install: "skipped"` with `installed` still read off the filesystem,
      # which is the one field `falsify` branches on.
      [ "{{probeInstall}}" != "true" ] && ARGS="$ARGS --no-install"
      # Each switch arrives as the literal "true"/"false"; absent means off.
      [ "{{probeLifecycleScripts}}" = "true" ] && ARGS="$ARGS --lifecycle-scripts"
      [ "{{probeTypecheck}}" = "true" ] && ARGS="$ARGS --typecheck"
      [ "{{probeCoverage}}" = "true" ] && ARGS="$ARGS --coverage"

      if ! "$FACTS" prepare $ARGS --never-fail; then
        fallback "the prepare process died without writing env.json (OOM, or a CLI that could not be executed) — dependencies may be absent or partial, so NOTHING downstream may read a thin analysis as evidence that there is nothing to find"
      fi
      echo "prepare: $(wc -c < "$OUT" 2>/dev/null || echo 0) bytes"

  - name: facts
    label: Facts · deterministic
    type: bash
    skip_if:
      - "analysisEnabled != true"
      - "scratch.reviewTriage.depth == 'light'"
    # `all_done`, not `all_success`: `prepare` SKIPS wherever probes are off,
    # and a skipped node is not `succeeded`.
    depends_on: [prepare]
    trigger_rule: all_done
    timeout_seconds: { from: factsTimeoutSeconds }
    command: |
      set -u
      FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
      OUT=.lastlight/pr-review/facts.json
      mkdir -p .lastlight/pr-review

      # The fallback envelope must VALIDATE — `seed` rejects a malformed one —
      # so that a failure reads downstream as "nothing was analysed" rather
      # than as "nothing was found".
      fallback() {
        printf '{"version":2,"generatedAt":"%s","extractor":"all","repo":"{{owner}}/{{repo}}","baseSha":"%s","headSha":"%s","tier":3,"engine":"none","languages":[],"coverage":"none","degraded":[{"extractor":"facts","reason":"%s"}],"toolchain":{"manifest":2,"bundled":{},"binaries":{}},"extractors":{}}\n' \
          "$(date -u +%Y-%m-%dT%H:%M:%S.000Z)" "${BASE:-unknown}" "$(git rev-parse HEAD 2>/dev/null || echo unknown)" "$1" > "$OUT"
      }

      # The MERGE BASE, never the base branch tip — a two-dot range attributes
      # every commit that landed on the base since the fork to this author. An
      # unresolvable base writes the fallback envelope; it never guesses.
      if ! BASE="$(git merge-base origin/{{baseBranch}} HEAD 2>/dev/null)" || [ -z "$BASE" ]; then
        fallback "no merge base between origin/{{baseBranch}} and HEAD (unrelated histories, or a clone too shallow to share one) — the diff range could not be established, so NOTHING here may be read as 'no findings'"
        echo "facts: no merge base — wrote a coverage:none envelope"
        exit 0
      fi

      # The shell-level catch covers what `--never-fail` cannot: a process that
      # DIES (OOM, a segfault in the native parser). `--stage-diff` writes the
      # per-file patches here, where the merge base is already resolved, so no
      # survey branch has to derive the range again.
      if ! "$FACTS" all --repo . --base "$BASE" --head HEAD --out "$OUT" --stage-diff --never-fail; then
        fallback "the analysis process died without writing an envelope (OOM, a segfault in the native parser, or a compiler binary that cannot exec) — NOTHING here may be read as 'no findings'"
      fi
      echo "facts: $(wc -c < "$OUT" 2>/dev/null || echo 0) bytes, base $BASE"
      # Logged separately so "nothing was staged" and "the branches never found
      # it" are two lines rather than one silence.
      if [ -s .lastlight/pr-review/diff/index.md ]; then
        echo "facts: staged $(ls .lastlight/pr-review/diff/*.patch 2>/dev/null | wc -l) per-file patch(es) under .lastlight/pr-review/diff/"
      else
        echo "facts: NO staged diff — every survey branch will have to derive the range itself"
      fi

  - name: seed
    label: Seed · obligations
    type: bash
    skip_if:
      - "analysisEnabled != true"
      - "scratch.reviewTriage.depth == 'light'"
    depends_on: [facts]
    trigger_rule: all_done
    timeout_seconds: { from: seedTimeoutSeconds }
    command: |
      set -u
      FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
      CONTRACT="{{obligationContract}}"
      MAX_OBLIGATIONS="{{maxObligations}}"
      # Read into a variable, never interpolated straight into the arg list: an
      # absent {{key}} renders EMPTY and would swallow the following flag.
      # Passed only when non-empty — the CLI refuses an empty --mint.
      MINT="{{mint}}"
      mkdir -p .lastlight/pr-review/hypotheses
      set -- seed --facts .lastlight/pr-review/facts.json \
        --out .lastlight/pr-review/obligations.json \
        --contract "${CONTRACT:-minimal}" \
        --max-obligations "${MAX_OBLIGATIONS:-48}" \
        --blocks .lastlight/pr-review/obligations
      if [ -n "$MINT" ]; then set -- "$@" --mint "$MINT"; fi
      "$FACTS" "$@" || true
      echo "seed: contract ${CONTRACT:-minimal} backstop ${MAX_OBLIGATIONS:-48} mint ${MINT:-none}"
      echo "seed: $(ls .lastlight/pr-review/obligations 2>/dev/null | wc -l) family block(s)"

      # A block is always emitted for all five seedable families, even a NOT
      # MEASURED one, so a MISSING line here is a seeder failure — and a survey
      # that then reports the family clean is reporting on no measurement at
      # all. `spec` is absent by design: it is built harness-side.
      for FAMILY in contract enforcement security state tests; do
        BLOCK=".lastlight/pr-review/obligations/$FAMILY.md"
        if [ -s "$BLOCK" ]; then
          echo "seed: $FAMILY block $(wc -c < "$BLOCK") bytes"
        else
          echo "seed: $FAMILY block MISSING — the survey branch for this family will run UNSEEDED"
        fi
      done
      exit 0

  # The six survey families, run CONCURRENTLY as one node: six agent sessions
  # in one provisioned workspace, one `executions` row each, so per-family
  # cost, resume and the dashboard column all survive. The families are
  # pairwise disjoint — each appends to its own `hypotheses/<family>.jsonl` —
  # so there is no ordering between them.
  #
  # Prompt order is fixed and expensive to change: the shared prefix (skills,
  # AGENTS.md, diff summary) goes first and the family's obligations last, so
  # the six share one provider-side cached prefix.
  - name: survey
    label: Survey · five families
    type: fanout
    skills: [survey-pass]
    model: "{{models.review-survey}}"
    # No survey pass executes code (issue #403): measured, the `spec` branch ran
    # the suite whatever the model. Every branch inherits this; a branch may
    # replace it whole with its own `command_policy`.
    # `host` (issue #404): measured, two branches scanned the whole disk
    # (`find / -name SKILL.md`) to recover a lost skill path.
    command_policy:
      install: block
      test: block
      host: block
      reason: >-
        No survey pass executes code. Cite CI from the Context section; where only
        running the code could settle a field of the evidence record, record it
        `unknown` — whether falsify probes the row is computed from that record.
        Nothing outside the checkout counts: never search the machine for a missing
        dependency or file — stop, and let the hypothesis go to falsify as unprobed.
    skip_if:
      - "analysisEnabled != true"
      - "scratch.reviewTriage.depth == 'light'"
    depends_on: [seed]
    trigger_rule: all_done
    # A CEILING the run clamps further per backend: gondolin boots a micro-VM
    # per session and pins to 1; `none` takes all six.
    max_concurrent: { from: surveyConcurrency, default: 6 }
    # The fan-out's OWN key. `on_soft_failure` is the `generic_loop` name and
    # at phase level the schema strips it silently, reverting to
    # `{retries: 0, then: fail}` — one degenerate turn then fails the review.
    on_branch_soft_failure: { retries: 1, then: complete }
    # A branch whose gate RAN and said no — seeded checks no row names, claim
    # rows with no evidence record, a zero-obligation family with no claim —
    # is re-run ONCE with the gate's output as the instruction, then gated
    # again. Without it the gate only recorded `condition_not_met` and the
    # half-done file went downstream as if complete (measured: MiniMax M3
    # answered 2 and 3 of 12 seeded checks on two of eight repeats). One extra
    # branch turn, only on failure.
    on_branch_gate_failure: { retries: 1 }
    # `context_file` is read by the HARNESS, host-side, from the cwd the
    # deterministic phases wrote in, and appended to the branch's prompt — the
    # model resolves no path of its own. An unreadable file appends a loud NOT
    # AVAILABLE notice, so a lost seed cannot pass for a clean family.
    #
    # `until_bash` checks every obligation carries a discharge code, not merely
    # that the file is non-empty. It DEGRADES to `test -s` when the obligations
    # are unreadable or the contract is `minimal`, because a gate an agent
    # cannot satisfy burns every iteration it has.
    branches:
      - name: contract
        prompt: prompts/survey-contract.md
        context_file: .lastlight/pr-review/obligations/contract.md
        until_bash: |
          FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
          "$FACTS" discharge --dir .lastlight/pr-review --family contract
      - name: enforcement
        prompt: prompts/survey-enforcement.md
        context_file: .lastlight/pr-review/obligations/enforcement.md
        until_bash: |
          FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
          "$FACTS" discharge --dir .lastlight/pr-review --family enforcement
      - name: security
        prompt: prompts/survey-security.md
        context_file: .lastlight/pr-review/obligations/security.md
        until_bash: |
          FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
          "$FACTS" discharge --dir .lastlight/pr-review --family security
      - name: state
        prompt: prompts/survey-state.md
        context_file: .lastlight/pr-review/obligations/state.md
        until_bash: |
          FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
          "$FACTS" discharge --dir .lastlight/pr-review --family state
      # No `tests` branch: the family has no seeder, so it is reported
      # `notMeasured` rather than run to write NOT MEASURED. `spec` is built
      # harness-side from the PR body and linked issues, so it reads
      # `{{specObligations}}` from the context instead of a block on disk —
      # there is nothing in obligations.json for `discharge` to grade, so it
      # runs `--ungraded`: the same canonical-id rewrite every branch gets
      # (issue #405), and the non-empty-file floor it always had.
      - name: spec
        prompt: prompts/survey-spec.md
        until_bash: |
          FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
          "$FACTS" discharge --dir .lastlight/pr-review --family spec --ungraded

  # The only phase that RUNS anything. It reads the hypothesis RECORDS and the
  # code, never an earlier pass's transcript — a checker that can see the
  # reasoning which produced a claim almost never rejects it.
  - name: falsify
    label: Falsify · run the probe
    prompt: prompts/review-falsify.md
    # Its OWN key, falling through to `models.review-survey`. Hard-wiring the
    # survey key meant the oracle's model could not be moved without moving all
    # five survey branches with it — two variables, so "is a cheap model good
    # enough at WRITING AND RUNNING a probe?" was not a runnable experiment.
    # The `{{#if}}` pair is load-bearing, exactly as on `adjudicate` below: a
    # bare unset `{{models.review-falsify}}` renders EMPTY, and an empty model
    # falls back to the DEFAULT model rather than to `models.review-survey`.
    model: "{{#if models.review-falsify}}{{models.review-falsify}}{{/if}}{{#if !models.review-falsify}}{{models.review-survey}}{{/if}}"
    skip_if:
      - "analysisEnabled != true"
      - "probesEnabled != true"
      - "scratch.reviewTriage.depth == 'light'"
    depends_on: [survey]
    trigger_rule: all_done
    # A WHOLE-PHASE ceiling, across every round. `probeRounds` bounds how many
    # times the loop may go round and not what it may spend, and this is the
    # one phase in the pipeline that executes code — so it is the one that
    # needs a wall-clock bound rather than a count. Like every other budget
    # here it names a context key, never a literal (#385).
    timeout_seconds: { from: falsifyTimeoutSeconds }
    # The one phase that runs code, and even here installing the repo is
    # `prepare`'s job, never the model's. Whether a TEST can run depends on
    # whether `prepare` installed the tree, so those two modes are read from
    # the run context: `probes: full` → `log` (a targeted test is the point;
    # a reach for the whole suite shows up in the logs), `probes: static` →
    # `block` (nothing is installed, so it cannot work). A scratch-dir install
    # (`npm install fastify@5` in /tmp/probe) follows the same switch.
    #
    # `host` (issue #404) is `log` here for one eval arm, then `block` once the
    # log shows nothing legitimate is caught: measured, 24 of 27 out-of-workspace
    # commands came from falsify hunting a dependency on the host (`find /`,
    # `~/.nvm`, a global `lastlight` put on PATH).
    command_policy:
      install: block
      install-scratch: { from: probeScratchInstallPolicy }
      test: { from: probeTestPolicy }
      host: log
      reason: >-
        Installing the repository's dependencies is the prepare phase's job, and the
        whole suite is never a probe. Run the one targeted check this hypothesis needs
        (a single test file, `node -e`, an AST or facts query), or record the
        hypothesis as unprobed.
    generic_loop:
      on_soft_failure: { retries: 1, then: complete }
      max_iterations: { from: probeRounds, default: 2 }
      # An EXISTENCE check, satisfiable in one honest pass: a hypothesis that
      # cannot be executed against is recorded `unprobed` and carried forward
      # with lowered confidence. Literal shell — `validateShellCommand`
      # rejects any command containing `{{`.
      until_bash: |
        FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
        "$FACTS" probes --dir .lastlight/pr-review

  # Writes findings to `.lastlight/pr-review/findings.json` — content only —
  # and never submits; `post-review` below does that. With the pipeline on the
  # brief collapses to one fast independent pass, since the surveys have
  # already done the deep work.
  - name: review
    label: Review
    # EXPLICIT: declaring `depends_on` anywhere disables the synthesized linear
    # chain for the WHOLE workflow, so without these two lines `review` and
    # `post-review` become roots and run in parallel with the surveys. And
    # `all_done`, because with `all_success` they would be skipped on every
    # deployment that has not enabled the pipeline.
    depends_on: [falsify]
    trigger_rule: all_done
    prompt: prompts/review.md
    skills: [pr-review, code-review]
    model: "{{models.review}}"
    variant: "{{variants.review}}"
    # Review re-created CI by hand (`npm ci` then `npm run lint:check && npm
    # test`) although `{{ciSection}}` already carries the result (issue #403),
    # so the suite is blocked everywhere. An INSTALL is blocked only when the
    # evidence pipeline is on: then `prepare`/`falsify` own execution and this
    # pass is an abbreviated read. With it off this is the whole review, and the
    # `pr-review` skill's install-to-probe affordance is a measured fix (it is
    # why the reviewer could open `node_modules/@slack` at all), so it stays.
    command_policy:
      install: { from: reviewInstallPolicy, default: allow }
      test: block
      host: block
      reason: >-
        This phase does not run the build, the linter or the test suite: CI already
        answered those, in the Context section. Cite it, or report the claim for the
        adjudicator to weigh. Nothing outside the checkout counts: never search the
        machine for a missing dependency — stop, and leave the claim unprobed.

  # #399 idea 2. One TypeSafe `systemOne` call PER HYPOTHESIS (not per
  # finding — hypotheses vastly outnumber findings, and per-hypothesis is the
  # volume a System-1 primitive is for), asking only the category axis
  # measured at AUC 0.897 (never correctness, measured dead at AUC 0.534).
  # Writes `.lastlight/pr-review/jev.json`, which `dossier` below renders as
  # an ADVISORY line per hypothesis — `adjudicate` still writes every
  # disposition itself. Measured 2026-09-22: 260 hypotheses / 8 cases, 83.6%
  # agreement with Sonnet's own adjudicate call on identical evidence,
  # $0.0053 — weakest on the rarest, highest-stakes categories, which is why
  # this annotates rather than decides.
  #
  # `dossier` depends on this PHASE (not just on the file it writes) so the
  # DAG itself enforces the ordering the render call needs — `jev.json` must
  # exist before `dossier --json`'s underlying render reads it.
  #
  # `|| true`: no `TYPESAFE_KEY`, a network error, a malformed answer — all of
  # it degrades to "no annotation" inside the CLI command itself (see
  # `jev-classify.ts`), and this phase must never fail the run on a third
  # party's API health that `legacy`/`dossier` deployments never needed at all.
  - name: jev-classify
    label: System-1 per-hypothesis pre-read
    type: bash
    skip_if:
      - "analysisEnabled != true"
      - "jevClassifyEnabled != true"
      - "scratch.reviewTriage.depth == 'light'"
    depends_on: [review]
    trigger_rule: all_done
    timeout_seconds: { from: jevTimeoutSeconds }
    command: |
      set -u
      FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
      "$FACTS" jev-classify --dir .lastlight/pr-review --repo . \
        {{#if jevModel}}--model "{{jevModel}}"{{/if}} || true

  # #399. Every record `adjudicate` needs, joined and quote-checked ONCE, by
  # code, instead of thirty shell calls inside the phase.
  #
  # Measured on the 8-case probes arm: `adjudicate` made 137 bash calls across
  # 8 adjudications — 35 turns and 30 bash on `1587-r2`, one `write`, about a
  # third of case cost — `cat`-ing hypothesis files `readHypothesisSet` had
  # parsed, transcripts `checkProbes` had located, the ledger twice, and dozens
  # of `sed -n '<N>p'` re-reads to check quotes it was handed.
  #
  # It goes to STDOUT as well as to disk, and that is the delivery mechanism:
  # `output_var` puts it in `{{phaseOutputs.dossier.output}}`, which
  # `review-adjudicate.md` interpolates. The alternative — a path in the prompt
  # — is the failure `context_file` exists to remove: of 120 non-spec survey
  # branches, 27 first-turn reads of a prompt-named path resolved against the
  # wrong root and hit ENOENT, and 23 never recovered. The on-disk copy is for
  # the eval harness's artifact capture and for the one case the dossier
  # deliberately does not inline in full, a truncated probe transcript.
  #
  # `|| true` and a fallback line for the same reason every deterministic phase
  # here has one: a non-zero exit fails the phase, and this is an input, not a
  # gate. An adjudication with no dossier is the shipped phase; an adjudication
  # that never ran is a review that never posts.
  - name: dossier
    label: Adjudication dossier
    type: bash
    skip_if:
      - "analysisEnabled != true"
      - "dossierEnabled != true"
      - "scratch.reviewTriage.depth == 'light'"
    # `jev-classify` too, not just `review` — `all_done` already tolerates a
    # skipped dependency (it is what `review` itself already relies on here;
    # the stricter `none_failed_min_one_success` below is for `adjudicate`,
    # which must not run if `review` genuinely FAILED). Declaring the
    # dependency is what makes the DAG run `jev-classify` to completion (or
    # skip) before this phase reads the file it writes.
    depends_on: [review, jev-classify]
    trigger_rule: all_done
    output_var: dossier
    timeout_seconds: { from: reconcileTimeoutSeconds }
    command: |
      set -u
      FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
      "$FACTS" dossier --dir .lastlight/pr-review --repo . \
        --out .lastlight/pr-review/dossier.md || true
      if [ -s .lastlight/pr-review/dossier.md ]; then
        cat .lastlight/pr-review/dossier.md
      else
        echo "DOSSIER NOT AVAILABLE — \`lastlight-facts dossier\` produced nothing."
        echo "Read .lastlight/pr-review/ yourself: hypotheses/*.jsonl, probes/verdicts.jsonl,"
        echo "probes/*.txt, findings.json, and \`lastlight-facts findings --ledger\`."
      fi

  # The exit of the funnel, and the only phase that can move recall: it ranks,
  # re-tiers and demotes the hypotheses into the review. It DELETES only when a
  # probe transcript refutes a claim.
  #
  # A SIBLING of `post-review`, not a link in its chain. `trigger_rule` is
  # per-node, so folding it into `post-review`'s deps would force that node to
  # `all_done` and lose the invariant that a FAILED REVIEW MUST NOT POST.
  # Declared first, so the sequential scheduler still runs it first.
  - name: adjudicate
    label: Adjudicate
    skills: [adjudicate-pass]
    prompt: prompts/review-adjudicate.md
    # The `{{#if}}` pair is load-bearing: a bare unset `{{models.review-adjudicate}}`
    # renders EMPTY, and an empty model falls back to the DEFAULT model rather
    # than to `models.review`.
    model: "{{#if models.review-adjudicate}}{{models.review-adjudicate}}{{/if}}{{#if !models.review-adjudicate}}{{models.review}}{{/if}}"
    variant: "{{variants.review}}"
    command_policy:
      install: block
      test: block
      host: block
      reason: >-
        This phase does not execute code. Weigh the probe transcripts falsify already
        wrote and the CI result in the Context section; an unsettled claim is demoted,
        not re-run. Nothing outside the checkout counts: never search the machine for
        a missing dependency — the hypothesis stays unprobed.
    skip_if:
      - "analysisEnabled != true"
      - "scratch.reviewTriage.depth == 'light'"
    # `none_failed_min_one_success`, NOT `all_success`, and the difference is
    # load-bearing in both directions:
    #
    #   - a FAILED review must still not post, and it does not: a failed dep
    #     fails this rule exactly as it fails `all_success`;
    #   - a SKIPPED `dossier` must not take this phase down with it, and under
    #     `all_success` it WOULD. `evaluateTriggerRule` requires every dep to be
    #     literally `succeeded`, and `skip_if` sets `skipped`
    #     (`scheduler.ts` — "Uses the same non-failing skip mechanics"), so on
    #     every deployment left on `review.analysis.adjudicate: legacy` the
    #     dossier skips and `all_success` would silently skip the adjudicator
    #     with it. The whole pipeline would go quiet with no phase failing.
    #
    # Declaring the dependency at all is what ORDERS the dossier before this
    # phase. The sequential scheduler happens to run the earliest-declared ready
    # node, so declaration order alone would work today — but a data dependency
    # that survives only until someone reorders the file is not a dependency.
    depends_on: [review, dossier]
    trigger_rule: none_failed_min_one_success
    generic_loop:
      on_soft_failure: { retries: 1, then: complete }
      max_iterations: 2
      fresh_context: true
      # The CONSERVATION gate: every hypothesis id must appear with exactly one
      # disposition, and a `dropped` one must name a transcript that EXISTS.
      # Silent omission is impossible by construction rather than by
      # instruction. Literal shell — no `{{` markers allowed.
      until_bash: |
        FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
        "$FACTS" findings --dir .lastlight/pr-review

  # The floor under that gate: reaching `max_iterations` unsatisfied is not a
  # phase failure in this engine, so hypotheses would otherwise go unaccounted
  # for. `--repair` writes every uncovered hypothesis into findings.json at
  # `internal` tier and promotes back any `dropped` entry whose transcript does
  # not exist. Idempotent, model-free, and exits 0 on every path.
  #
  # It is also where a finding's SEVERITY is derived (issue #405): every
  # hypothesis-derived finding gets the severity its evidence records and probe
  # verdicts imply (`finding-severity.ts`), stamped over whatever `adjudicate`
  # wrote — which is the mechanism behind "the adjudicator does not choose it".
  # `post-review` ranks both posting budgets on that value.
  - name: reconcile
    label: Reconcile · the conservation floor
    type: bash
    skip_if:
      - "analysisEnabled != true"
      - "scratch.reviewTriage.depth == 'light'"
    depends_on: [adjudicate]
    # `all_done`: the floor must apply even when the adjudicator failed — that
    # is precisely when there is something to repair.
    trigger_rule: all_done
    timeout_seconds: { from: reconcileTimeoutSeconds }
    command: |
      set -u
      FACTS="${LASTLIGHT_FACTS_BIN:-$(command -v lastlight-facts || echo /opt/lastlight/bin/lastlight-facts)}"
      if ! "$FACTS" findings --dir .lastlight/pr-review --repair; then
        echo "reconcile: could not repair findings.json; it stands as the review phase wrote it"
      fi
      exit 0

  # In-process submission. Reads findings.json for the review CONTENT and
  # supplies the rest from the harness itself — PR number, base ref, head SHA
  # and diff — so the agent never hand-copies metadata. Anchors each finding to
  # a changed line, demotes off-diff findings to the body, and posts one
  # review. Idempotent on resume.
  - name: post-review
    label: Post inline review
    type: post-review
    depends_on: [review]
