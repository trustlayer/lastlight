# Repositories Last Light manages. Set an explicit list in your private overlay
# ($LASTLIGHT_OVERLAY_DIR/config.yaml), which replaces this one wholesale;
# webhooks for repos not listed are filtered at the connector level. When left
# EMPTY (the default), the managed list is instead sourced from the GitHub App
# installation itself — the repos it can access, discovered at boot and kept
# live by installation webhooks — so an org install that already limits the App
# to a subset of repos need not maintain a second copy here. See
# src/managed-repos.ts.
managedRepos: []

# The GitHub App slug (no `[bot]` suffix). Single source of truth for the bot's
# identity: the incoming `@mention` handle (e.g. `@last-light`), the derived
# `botLogin` (`<botName>[bot]`, self-comment/self-review filter), and the git
# commit author. Override in your overlay `config.yaml` or via the
# `GITHUB_APP_BOT_NAME` env var — e.g. set `nearform-lastlight` for the
# Nearform app so it triggers on `@nearform-lastlight`.
botName: last-light

models:
  default: anthropic/claude-sonnet-4-6
  # The five `pr-review.yaml` survey branches and `falsify`. SET, not an
  # example — see the note below: this is the measured RECALL winner for
  # hypothesis generation, not a cost downgrade, and leaving it unset silently
  # ran six Sonnet phases under every number the pipeline was tuned on. A
  # deployment whose `models.default` points at a non-Anthropic provider MUST
  # override this in its overlay `config.yaml`, or these six phases reach for a
  # provider it has no key for.
  review-survey: anthropic/claude-haiku-4-5-20251001
  # The `triage` phase of `pr-review.yaml` — "how much review is this push
  # owed?". SET, not an example: the phase exists to save spend, so it must
  # never default to the deployment's main review model. Same tier as
  # `review-survey`, and the task is smaller — read a bounded diff, answer with
  # one word.
  review-triage: anthropic/claude-haiku-4-5-20251001
  # Per-task overrides keyed by phase or helper name, e.g.:
  #   architect: anthropic/claude-opus-4-8
  #   classifier: anthropic/claude-haiku-4-5-20251001   # the intent-routing helper
  #   screener: anthropic/claude-haiku-4-5-20251001     # the prompt-injection screener
  #   diagnose: anthropic/claude-haiku-4-5-20251001     # the CI-failure classifier
  #   pr-fix-retry: anthropic/claude-opus-4-8           # used above fix.escalateModelAfterAttempt
  #   review-adjudicate: anthropic/claude-sonnet-4-6      # the adjudication pass
  #   review-summary: openai/gpt-5.4-mini                # post-review's summary, written after the caps
  #   review-falsify: anthropic/claude-haiku-4-5-20251001 # the probe oracle
  # A per-task entry wins over `default`. `classifier`/`screener` are the cheap
  # one-shot helpers (src/engine/llm.ts); when unset they fall back to the first
  # configured provider's fast model, NOT `default`.
  #
  # `diagnose` is worth pinning. It is the first phase of both fix workflows and
  # it only reads logs and reproduces a command — it writes no code — so a cheap
  # model is the point of splitting it out from `fix` at all. Left UNSET here
  # because these examples name Anthropic models and a deployment that overrides
  # only `default` to another provider would otherwise find this phase reaching
  # for a provider it has no key for. Unset, it falls through to `default`, so
  # each fix run costs one extra call at the default model until you pin it.
  #
  # `review-survey` is SET above, and it is the one entry in this block that is
  # not an example. It is the key the five survey branches of `pr-review.yaml`
  # declare (`model: "{{models.review-survey}}"`, one per obligation family),
  # and the `falsify` phase's fall-through.
  #
  # Pinning it to Haiku 4.5 is NOT a cost downgrade, and that is the unusual
  # part: on review RECALL, Haiku 4.5 BEATS Sonnet 4.6 on two independent evals
  # — 41.2% vs 22.1% on the Martian corpus. It is the better model for
  # HYPOTHESIS GENERATION, which is what a survey branch does; its price is
  # merely what makes a five-way fan-out affordable inside the pipeline's 2–3x
  # budget. It is a different question from the SAYING phases (`review`,
  # `adjudicate`), which are not pinned here.
  #
  # It was deliberately left unset for a long time, for the `diagnose` reason
  # above — these are Anthropic model ids, and a deployment that overrode only
  # `default` to another provider would find six phases reaching for a provider
  # it has no key for. That reason has not gone away; it is now handled the
  # other way round, because the cost of the unset default turned out to be
  # worse. Unset, all six fell through to `default` (Sonnet) SILENTLY, so an
  # operator who enabled the pipeline ran a configuration no measurement in
  # `docs/plans/deterministic-pr-levers.md` describes — every recall, cost and
  # latency number on record assumes Haiku surveys. A wrong-provider failure is
  # loud on the first run; a quietly-different model is not.
  #
  # SO: a non-Anthropic deployment must override this key in its overlay
  # `config.yaml` (a cheap, fast model of its own provider), exactly as it
  # already overrides `models.default`.
  #
  # `review-falsify` is the ORACLE's own key — the one phase that runs code.
  # Unset, it falls through to `review-survey` (its template is the same
  # `{{#if}}` pair `adjudicate` uses, so an unset key lands on the survey model
  # and not on `default`), which is exactly the behaviour before the key
  # existed. It exists so the oracle's model can be moved on its own: it was
  # hard-wired to `review-survey`, so asking "is a cheap model good enough at
  # WRITING AND RUNNING a probe?" meant moving all five survey branches too —
  # two variables, and therefore not a runnable experiment.
  #
  # `review-adjudicate` is the adjudicate phase's own key in `pr-review.yaml` —
  # the RANKING pass over what the surveys found, a different task from survey
  # discovery, so it can be moved independently of `models.review`. Unset, it
  # falls through to `models.review` (the phase's template guards it), so a
  # deployment that never sets it keeps today's behaviour exactly.

# Provider endpoints. Ships EMPTY — every provider keeps the endpoint baked into
# the registry (packages/shared/src/providers.ts), which is exactly today's
# behaviour. Override in your overlay `config.yaml` to run the model calls
# through your own LLM gateway (central spend accounting, key custody, rate
# limiting, audit) or to reach a self-hosted / Azure-fronted model the registry
# has no entry for:
#
#   providers:
#     anthropic:                                 # a gateway speaking Anthropic's
#       baseUrl: https://gateway.internal/anth   #   dialect: only the URL moves
#     acme:                                      # a provider we've never heard of
#       baseUrl: https://llm.corp.example/v1     #   required — nothing to inherit
#       api: openai-completions                  #   or anthropic-messages
#       envKey: ACME_API_KEY                     #   defaults to <PREFIX>_API_KEY
#
# On a BUILT-IN provider you may also re-point `envKey` (a gateway holding its
# own credential), but NOT `api` — a built-in's dialect is fixed, and a gateway
# that speaks a different one is a provider of your own (the `acme:` shape).
#
# The URL is routing, not a secret, so it belongs in this file; the key stays in
# secrets/.env. The sandbox egress allowlist follows the URL automatically.
# https is required off loopback. Env equivalents: ANTHROPIC_BASE_URL /
# OPENAI_BASE_URL / … and LASTLIGHT_PROVIDERS (JSON). See spec/02-configuration.md.
providers: {}

variants: {}

otel:
  enabled: false
  serviceName: lastlight
  includeContent: false
  forwardToSandbox: true
  strict: false
  collectorHosts: []

sandbox:
  backend: gondolin
  maxTurns: 200
  # Wall-clock budgets (issue #385). This file is the ONLY place a timeout
  # default lives — code carries no numeric fallback, and a missing or invalid
  # key fails config load naming the key. Override in your overlay.
  agentTimeoutSeconds: 1800     # one agent phase run, when the phase sets no timeout_seconds
  commandTimeoutSeconds: 300    # a type: bash / type: script phase with no timeout_seconds
  untilBashTimeoutSeconds: 30   # a generic_loop until_bash check with no timeout_seconds

# The build/test GATE budget (issue #385) — one number for "a single full
# build/test gate command" (install excluded), shared by the fix loops'
# until_bash gate and the agent's own gate commands (core passes it to
# agentic-pi as --gate-timeout on every agent run). Seeded on the run's
# template context as {{gate.timeoutSeconds}}.
#
# `timeoutSeconds` is REPO-SETTABLE — only the repo knows how long its suite
# takes — which departs from the "a repo may only be more conservative" rule,
# so it is clamped to `maxTimeoutSeconds`: min(repo, operator max).
# `maxTimeoutSeconds` and `phaseTimeoutSeconds` are OPERATOR-ONLY.
# Validated at load: timeoutSeconds <= maxTimeoutSeconds < phaseTimeoutSeconds.
#
# `fix.gateTimeoutSeconds` is the DEPRECATED spelling of `timeoutSeconds`: an
# overlay that still sets it (and not gate.timeoutSeconds) is mapped across,
# with a warning.
gate:
  timeoutSeconds: 900           # budget for ONE full build/test gate command (install excluded)
  maxTimeoutSeconds: 1500       # ceiling a repo's .lastlight/lastlight.yml gate.timeoutSeconds is clamped to
  phaseTimeoutSeconds: 2400     # budget for a phase that runs a gate (guardrails gate, executor); must exceed maxTimeoutSeconds

# Where the per-phase build handoff docs (architect-plan.md, status.md,
# executor-summary.md, reviewer-verdict.md, …) live:
#   repo   — committed into the target repo under .lastlight/issue-N/ (default,
#            the docs ride the working branch and link to GitHub blob URLs).
#   server — persisted on the Last Light server under $STATE_DIR/build-assets/,
#            injected into each phase from outside the repo (never committed)
#            and linked to the admin dashboard.
buildAssets:
  location: repo

# State database. `url` is either a libsql-style URL
# ("file:/app/data/lastlight.db", ":memory:") or a "postgres://…" URL for an
# external/managed Postgres. null/absent → `file:` + the DB_PATH /
# $STATE_DIR/lastlight.db resolution, i.e. exactly the pre-Drizzle behaviour,
# and the recommended default: SQLite needs nothing running.
#
# ⚠ PUT A postgres:// URL IN `DATABASE_URL` (secrets/.env), NOT HERE. This file
# and your overlay's config.yaml are VERSION-CONTROLLED and pushed to a GitHub
# remote; the dashboard masks the credential at render time, which cannot
# un-commit anything.
#
# `driver` picks how the postgres:// dialect is carried: "pg" (node-postgres, a
# standard TCP pool — self-hosted, RDS, Cloud SQL, Supabase) or "neon" (Neon's
# serverless WebSocket pool). Absent → auto-detected from the host, so only a
# Neon database behind a custom domain needs it. `poolMax` bounds the pool
# (default 10). Last Light is a single-writer process — Postgres here is a
# storage choice, not multi-instance HA.
#
# Env overrides: DATABASE_URL, DATABASE_DRIVER, DATABASE_POOL_MAX.
database:
  url: null
  driver: null
  poolMax: null

# Global concurrency limiter (issue #172). Caps how many sandboxed workflow
# runs execute at once; excess triggers are persisted as `queued` in
# workflow_runs and admitted when a slot frees. Resumes/orphan restarts bypass
# the cap (finish in-flight work first). Queued runs older than
# maxQueueWaitMs are cancelled with a "waited too long" notice.
concurrency:
  maxWorkflows: 4          # env override: MAX_CONCURRENT_WORKFLOWS
  maxQueueWaitMs: 3600000  # 1 hr; env override: MAX_QUEUE_WAIT_MS

# Sandbox-workspace reaping (issue #106). Every task clones the target repo into
# $STATE_DIR/sandboxes/<taskId>/; the harness now owns cleanup of those clones so
# the disk stays bounded under cron-review churn (this replaces the out-of-band
# host cron). `reapOnCompletion` deletes an ephemeral run's workspace the moment
# it finishes successfully; reusable per-PR workspaces (pr-review/pr-fix/build)
# are kept as a warm cache and bounded only by the sweep below. The sweep is the
# backstop for failed/crashed leftovers: it removes non-live dirs older than
# retentionHours and, if more than maxDirs remain, evicts the oldest (LRU). A dir
# whose sandbox container is still running is never removed.
# Feedback signals (issue #255) — a 👍/👎 on something Last Light wrote, scored
# against the workflow run that wrote it, so the effect of prompt/skill changes
# is measurable over time.
#
# Slack is LIVE (reaction_added is a real event) and on by default. GitHub sends
# no webhook for reactions at all, so it has to be polled — that half is opt-in.
# The cost is bounded by the data, not the schedule: we poll individual bot
# comments ("anchors"), never issues, each retires after windowDays, and
# maxAnchorsPerTick/100 is exactly how many GraphQL requests a tick can make
# (one rate-limit point each, against a 5000/hour budget).
feedback:
  enabled: true              # master switch (Slack signals, live)
  github: false              # opt into the GitHub reaction poller
  pollSchedule: "*/30 * * * *"
  windowDays: 14             # how long an anchor stays pollable
  maxAnchorsPerTick: 500     # = 5 GraphQL requests per tick
  retentionDays: 90          # anchors pruned after this; signals kept
  otel: true                 # export each signal as a span on the run's trace

# The optional "my teams' repos" dashboard filter (issue #169).
#
# Offers each GitHub-authenticated user a header toggle that narrows every list
# to the managed repos their GitHub org teams own. **Opt-in and off by default**
# — nobody's view changes until they choose it, and the choice is remembered per
# browser.
#
# Opt-in is load-bearing, not timidity. Team grants describe INVOLVEMENT, not
# access: an org owner reaches every repo without a team grant anywhere. Applied
# as a default this hides people's own work — measured against the live nearform
# install, one maintainer's single team covered 4 of 8 managed repos and hid
# `nearform/lastlight`, which they own. Applied as a filter somebody switched on,
# the same narrowing is exactly the decluttering they asked for.
#
# The filter is your teams' repos PLUS the managed repos your own account owns.
# Teams are an org concept, so a personal repo can never be granted by one; a
# strictly team-derived answer hid every personal repo an instance managed. The
# test is `owner == your login` — not "the owner isn't an org", which would put
# other people's personal repos in your filter.
#
# This flag is the OPERATOR switch: it needs the GitHub App's organization
# `Members: read` permission, so turn it on once you've granted that and
# re-consented the App on each installation. Without it the resolver fails open
# and nothing is filtered — the toggle still appears and says why, so an
# outstanding setup step is visible instead of silent. UI only — every list
# endpoint still returns global data, and each budget below fails OPEN (shows
# more).
#
# Nothing is crawled up front. A user's teams are resolved on demand, on their
# first dashboard request, and cached — so an org with thousands of repos and
# hundreds of teams costs a handful of GraphQL requests per logged-in person,
# not a full-org walk.
teamVisibility:
  enabled: false             # operator switch — needs the App's `Members: read`
  ttlMinutes: 60             # how long a resolved answer is reused
  maxTeamsPerUser: 50        # more teams than this ⇒ fail open
  maxPagesPerTeam: 20        # 100 repos/page; a bigger grant ⇒ fail open
  maxRequestsPerResolve: 60  # absolute ceiling for one cache miss

cleanup:
  sandbox:
    enabled: true              # master switch for the TTL/LRU sweep
    reapOnCompletion: true     # reap ephemeral workspaces on terminal success
    sweepSchedule: "0 * * * *" # hourly
    retentionHours: 12         # sweep dirs older than this
    maxDirs: 40                # LRU cap on total workspace dirs

# Pin which core version an instance runs. Set `deploy.version` to a git tag/ref
# (e.g. v0.10.6) in your private overlay's config.yaml — `lastlight server
# update|setup` then checks core out at that tag and rebuilds, instead of
# tracking `main`. Unset here (and the sentinels `main`/`latest`) means track
# `main` — today's behaviour. Overridable via the `LASTLIGHT_CORE_VERSION` env
# var for CI. Bump + commit in the overlay to drive deploys declaratively.
deploy:
  version: null

approval: {}

# Per-repository configuration (issue #180). A managed repo may commit a
# `.lastlight/` directory — mirroring the instance overlay's on-disk shape
# (`lastlight.yml`, `workflows/prompts/*.md`, `skills/<name>/SKILL.md`,
# `agent-context/*.md`) — that overrides a BOUNDED subset of this config for
# runs against that repo. Precedence: default -> overlay -> env -> repo.
#
# The block below is the OPERATOR's bounds on that layer; a repo can never widen
# them. It is inert out of the box: nothing changes for an existing deployment
# until a repo actually commits `.lastlight/lastlight.yml`.
#
# SECURITY: the repo layer is ALWAYS read from the repo's DEFAULT BRANCH, never
# a PR head and never the sandbox checkout — otherwise a PR could rewrite the
# configuration of the agent reviewing it. See src/config/repo-config.ts.
repoConfig:
  enabled: true                  # master switch; false ignores every repo's .lastlight/
  # Config keys a repo may set, as dotted paths. Anything outside this list is
  # dropped with a warning (the run still proceeds — a repo's config file can
  # never fail a run). Narrow it to tighten the blast radius. Removing `crons`
  # makes the operator's `crons:` block below un-overridable — the kill switch.
  # `fix` / `dependencies` / `review` are clamped one-way (see those blocks): a
  # repo can only ever be MORE conservative than the values set here.
  # `notifications` is the exception to that rule — it is ROUTING (which Slack
  # channel this repo's digest goes to), and a channel has no more/less
  # conservative direction, so the repo's value simply wins. What bounds it is
  # the default-branch trust rule above plus Slack itself: the bot can only post
  # where it has been invited. Drop `notifications` from this list to take
  # channel choice back.
  # `services` is a third shape again: a CAPABILITY GRANT. A repo declaring a test
  # postgres is not asking to be looser than an operator value, it is making a request
  # measured against `allowedImages` below. Drop it from this list to refuse the whole
  # feature outright.
  allowKeys: [models, variants, crons, disabled.workflows, disabled.crons, approval,
              fix, dependencies, review, notifications, services, gate]
  allowedModels: null            # null = any model whose provider prefix is configured;
                                 # a list restricts repos to exactly those model specs
  allowAssets: true              # allow the repo's prompt/skill/agent-context overrides
  # Container images a repo may run as dependency services, registry-qualified.
  # NOTE the polarity is the INVERSE of allowedModels: [] / null denies EVERY image.
  # A service image is arbitrary code pulled onto this host, outside the sandbox's
  # egress policy, so the feature stays inert until you list images here. Example:
  #   allowedImages: ["docker.io/library/postgres:*", "mcr.microsoft.com/mssql/server:*"]
  allowedImages: []
  maxServices: 2                 # ceiling on dependency services per phase

bootstrap:
  label: "lastlight:bootstrap"

# The HOLD label. Apply it to ANY issue or pull request and Last Light stops
# acting on that subject entirely — no triage, no review, no fix, no merge, no
# comment. Remove it and the bot resumes; there is no record to clear either way.
#
# It outranks everything except "we could not read the pull request at all",
# including an explicit `@<bot> ...` request — which gets exactly one reply
# naming the label, so a direct instruction is refused rather than ignored.
# `requires-human` is a NOTIFICATION ("I stopped, a human should look") and is
# read by nothing; this is the live precondition.
#
# Renaming it here changes what the CODE gates on. The packaged prompts create
# the DEFAULT name in their `github_ensure_labels` pass, so a rename also wants
# a forked prompt (or the label created by hand). Env: LASTLIGHT_HOLD_LABEL.
hold:
  label: lastlight-ignore

# The issue label filter (fork patch). When `requiredLabels` is not empty, Last
# Light starts a workflow in `workflows` on an issue only when the issue has one
# of the labels. The label comparison ignores the case. An empty list turns the
# filter off. A repo-wide triage scan has no issue number, so the filter does
# not apply to it. Example: `requiredLabels: [bug]` stops the triage and the
# build of an issue that has no `bug` label.
issueFilter:
  requiredLabels: []
  workflows: [issue-triage, build]

explore:
  defaultRepo: null

review:
  postsCheck: false            # post the `last-light/review` Check Run
  # When a pr-review run is triggered:
  #   eager        — on pr.opened / synchronize / reopened, in parallel with CI
  #   after-checks — once the head SHA's checks SETTLE, either colour (default:
  #                  the review can then read and cite the CI result, and a
  #                  push-storm collapses to one review per settled SHA)
  #   on-request   — never automatically; only when explicitly asked for
  trigger: after-checks
  requestLabel: null           # label that requests a review in `on-request` mode
  skipDraft: true              # skip draft PRs (what the review cron has always done)
  # Paths whose contents are DERIVED, not authored (issue #271). When every path
  # changed since our last posted review matches one of these, the re-review is
  # skipped — a lock-file re-derivation is a new head SHA, and per-head dedup
  # alone made that a second, byte-identical formal review.
  #
  # Never suppresses a FIRST review, an explicit `@bot review`/request label, or
  # a push that also touched a hand-written file. Set to `[]` to turn the gate
  # off. Patterns: `*` stops at a `/`, `**` crosses one, and a pattern with no
  # `/` matches a basename anywhere in the tree.
  #
  # Repo-settable, SUPERSET-only — a repo may add its own generator output, but
  # dropping one of these entries buys it more review runs and is restored.
  generatedPaths:
    - "*.lock"
    - package-lock.json
    - npm-shrinkwrap.json
    - pnpm-lock.yaml
    - yarn.lock
    - bun.lockb
    - go.sum
    - "*.min.js"
    - "*.min.css"
    - "*.generated.*"
    - "**/__generated__/**"
  # Skip a re-review whose EFFECTIVE DIFF is unchanged (issue #378).
  #
  # `generatedPaths` above asks "is the delta since our last review all derived
  # output?". This asks the other question: is the PR's own three-dot diff
  # (`base...head`) byte-identical to the one we already reviewed? A
  # `Merge branch 'main' into feature` push changes no line the author wrote,
  # but it is a new head SHA, and the delta since the last review is every file
  # main brought in — so nothing above could ever suppress it. It also covers a
  # rebase that preserves the tree and an empty force-push, and it correctly
  # does NOT fire when the base touched a file the PR also touches, because the
  # merged patch then genuinely differs.
  #
  # Never suppresses a FIRST review or an explicit `@bot review` / request
  # label / check Re-run. Every degraded read — GitHub truncating the compare at
  # 300 files, a binary file with no patch, a failed request — dispatches a full
  # review. Set to `false` to turn the gate off.
  #
  # Repo-settable DOWNWARD only: a repo may turn it off and buy itself more
  # review runs, never on over an operator who turned it off.
  skipUnchangedDiff: true
  # The DEPTH TRIAGE phase (issue #378) — one cheap pass at the head of
  # `pr-review.yaml`, on a RE-REVIEW only.
  #
  # Between "the diff did not move at all" (which `skipUnchangedDiff` suppresses
  # outright) and "this is a substantial new push" sits a real but small delta.
  # Re-running the full evidence pipeline over it costs about what the first
  # review cost — the survey fan-out is ~75% of a review's spend and ~90% of its
  # branch-seconds, and on the gondolin backend the branches run serially — to
  # re-derive findings that have not changed.
  #
  # The phase reads the diff since our last posted review and ends with one
  # `REVIEW_DEPTH: full` or `REVIEW_DEPTH: light` line. On `light`, the seven
  # analysis phases skip and the review prompt renders a focused single pass
  # that names the prior verdict. On `full`, on no marker, on a first review, or
  # with this switch off, the run is exactly what it is today.
  #
  # ON by default, unlike `analysis:` below. It is not the same trade: the
  # pipeline is an addition that buys more analysis, while triage can only
  # REMOVE work from a run that would otherwise have happened in full, and its
  # worst case is today's review plus one short prompt.
  #
  # OPERATOR-ONLY, same reasoning as `analysis:` — it is spend, and there is no
  # "more conservative" direction for a repo to clamp it toward.
  triage:
    enabled: true
    timeoutSeconds: 300
  # The review evidence pipeline (docs/plans/deterministic-pr-levers.md).
  #
  # OFF by default and that is a locked decision: `enabled: false` reproduces
  # today's two-phase review BYTE-FOR-BYTE — every variable this block governs
  # is absent from the template context rather than present-and-empty. Each
  # stage lands dark and is switched on per deployment once it is measured.
  #
  # What `enabled: true` adds today (WP0, the `spec` axis):
  #   - `{{prBody}}` + `{{linkedIssues}}` — the PR body and the issues it closes
  #     (`closingIssuesReferences`), neither of which reached a prompt before.
  #   - `{{specObligations}}` — one obligation per acceptance criterion found in
  #     the issue/PR body, each naming BOTH ENDS of the mechanism: the criterion
  #     itself, and the changed files that could satisfy it with `found: false`.
  #     A one-ended seed measures WORSE than no seed (IRIS ablation: -3), which
  #     is why the second end is the mechanical changed-file list and never
  #     prose.
  #   - the split verdict: `findings.json` may carry
  #     `verdict: { spec, standards }` and a `fail` on either axis stops the
  #     review being an APPROVE, so a clean standards pass can no longer hide a
  #     spec miss (issue #271 fix 7).
  #
  # OPERATOR-ONLY — unlike every other `review:` key, a repo may not set this.
  # It is spend, with no "more conservative" direction to clamp towards.
  # What `enabled: true` additionally adds from WP3 (the facts-derived families):
  #   - a `facts` phase writing .lastlight/pr-review/facts.json (no model spend)
  #   - a `seed` phase turning it into obligations.json + one rendered block per
  #     family, each naming BOTH ENDS of a mechanism
  #   - five survey branches, one per live family, each appending to its OWN
  #     hypotheses/<family>.jsonl — no pass ever reads or rewrites another's,
  #     which makes consensus collapse impossible by construction rather than
  #     by instruction
  #
  # Neither phase can run where `lastlight-facts` is not resolvable. That is
  # true of the SANDBOX IMAGE today (WP2 is what installs it); the eval harness
  # runs `--sandbox none` on the host, where it is. Set LASTLIGHT_FACTS_BIN to
  # point at a specific build. A missing binary degrades to a `coverage: "none"`
  # envelope and the survey phases say so — it never fails the run.
  analysis:
    enabled: false
    # A SAFETY BOUND, not a budget — it should never bind on a real PR. The
    # extractor still ranks, truncates and records what it dropped, but this
    # number exists only to stop a pathological PR blowing the prompt.
    #
    # It was 6, and while the spec axis produced nothing that was inert. The
    # moment the axis started working it became the binding constraint on FIVE
    # of the six linked cases in the gate set — 11 criteria extracted and 6 kept
    # on the three `1587` cases, 10 and 6 on the two `1641` ones. What it was
    # discarding is the acceptance-criteria checklist a human wrote on the
    # issue, which is the most direct statement of intent this pipeline ever
    # gets. Capping GENERATION also inverts locked decision 2: we deliberately
    # over-generate and let the probe oracle and WP6b's attention boundary
    # (maxInlineComments + the family thresholds) do the narrowing. Truncating
    # obligations truncates DISCOVERY, and discovery is the measured ceiling.
    maxSpecObligations: 40
    # The five facts-derived families are bounded PER FAMILY, not pooled:
    # contract 12, enforcement 12, state 8, security 8, tests 8 (FAMILY_CAPS in
    # packages/code-facts/src/seed.ts). Each family's obligations feed exactly
    # ONE survey branch, so the cost is per branch and a pooled cap never bound
    # the fattest one; and cross-family ranking prices incommensurable mechanism
    # classes against each other — measured, `contract` minted 89 across the
    # eight gate cases while `security` minted 3, and the pool went to contract.
    #
    # This key is the TOTAL BACKSTOP applied after those ceilings, and it is
    # their sum — so it cannot bind on a shipped configuration. It exists so
    # that raising one ceiling is a bounded act rather than an unbounded one.
    maxObligations: 48

    # Which obligation BLOCK the six families are handed. `full` | `minimal`.
    #
    # This is a CONTROL, not a dial. On 2026-08-23 the block gained a mandatory
    # discharge contract (a `discharge` field the row can actually hold a code
    # in), an un-truncated id checklist and one worked exemplar. Measured on
    # `prreview__skillspro-1587-r2` with `--repeats 3`: discharge compliance went
    # 0/33 to 33/33, and the union of matched gold went 4-of-5 to ZERO-of-5, all
    # three repeats. Half to two thirds of every hypothesis became a "clean
    # quote" — QUOTE with `failureScenario: null`, i.e. "I found the line and it
    # is fine".
    #
    # Two variables moved in that one commit, so it cannot say which caused it:
    # the obligations may ask the WRONG QUESTION (and making a wrong question
    # mandatory turns hunting into checklist-clearing), and/or RELIABLE SEEDING
    # itself suppresses discovery — the same commit stopped ~24% of survey
    # branches losing their seed entirely.
    #
    # `minimal` is the SHIPPED default, and it was measured in, not assumed:
    # under `full`, half to two-thirds of everything the surveys produce is a
    # clean quote — "I looked, the line is fine" — and those verification
    # reports reached real pull requests as posted findings. Under `minimal`
    # the same pipeline posted the same recall union with 37–71% better
    # signal-to-noise and half the run-to-run variance. Under `minimal`,
    # `lastlight-facts discharge` degrades to the `test -s` floor it replaces —
    # the block does not ask for discharge codes, so grading them would fail
    # every family over a field the survey was never told to write.
    #
    # `full` remains available for deployments that want the discharge-code
    # telemetry (and the clean-discharge demotion it enables at the posting
    # boundary), at the cost of the verification-report volume above.
    obligationContract: minimal
    # What `adjudicate` is handed, and what shape it writes back (#399).
    #
    #   legacy   — the shipped phase. The prompt names the files and the model
    #              shells out to assemble them, then writes a `tier` and a
    #              `confidence` per finding.
    #   dossier  — a deterministic `dossier` phase renders every record the
    #              phase needs and the harness attaches it, and the model writes
    #              typed attributes (`claim` / `category` / `fix`) from which a
    #              pure function derives the tier. `confidence` is not asked for.
    #
    # ONE key for both halves deliberately: they move the same phase's measured
    # surface — its input and its output — so shipping them together costs one
    # comparability break with the archive instead of two, and one arm validates
    # both.
    #
    # What it targets, measured on the 8-case probes arm: adjudicate spends 137
    # bash calls across 8 adjudications (35 turns, 30 of them bash on the stress
    # case) re-deriving records the harness already holds — about a third of
    # case cost — and makes its actual judgement at the end of that transcript.
    #
    #   jev      — implies `dossier` (same rendering, same typed output) and
    #              additionally runs `jev-classify`: one TypeSafe `systemOne`
    #              call PER HYPOTHESIS (not per finding — hypotheses vastly
    #              outnumber findings, and per-hypothesis is the volume a
    #              System-1 primitive is for), asking only the category axis
    #              measured at AUC 0.897 (never correctness, measured dead at
    #              AUC 0.534). Its category is rendered into the dossier as an
    #              ADVISORY line — Sonnet still writes every disposition
    #              itself. Measured 2026-09-22: 260 hypotheses / 8 cases,
    #              83.6% agreement with Sonnet's own call on identical
    #              evidence, $0.0053. Weakest on the rarest, highest-stakes
    #              categories (`defect` 33%, `correctness-risk` 40% recall
    #              against Sonnet) — that is why it annotates rather than
    #              decides. Needs `TYPESAFE_KEY` (or `TYPESAFE_API_KEY`); its
    #              absence degrades the phase to "no annotation", never a
    #              failed run.
    #
    # `legacy` until an arm has run for each. An unrecognised value lands there
    # too.
    adjudicate: legacy
    # The TypeSafe model `jev-classify` calls, under `adjudicate: "jev"`.
    # `null` = the CLI's own default (`TYPESAFE_MODEL` env, else `jev-latest`).
    # Kept out of the `models:` map above — TypeSafe is a distinct provider
    # from the `provider/model` chat models that map resolves.
    jevModel: null

    # Which D2 minting rules `lastlight-facts seed` runs beyond the original
    # four. A comma-list; the valid tokens are `all-in-diff` (contract
    # obligations for symbols whose every reference is inside the diff) and
    # `registrations` (security obligations for route/hook registration
    # order). BOTH ON by default — the measured shipped shape (8-case confirm:
    # internal paired +10/−1, p=0.006, the only lever measured to GROW the
    # recall union rather than rotate it; external validation +7/−0, p=0.008).
    # Empty string = the pre-D2 baseline set. It reaches the sandbox as
    # `--mint <spec>` on the seed phase's command line — appended only when
    # non-empty — and the CLI refuses any unknown token with exit 2, so a
    # typo'd arm is loud rather than silently running baseline.
    mint: all-in-diff,registrations

    # RECORDED, NOT CONSUMED. The fan-out's branch list in `pr-review.yaml` is
    # the authority (five branches; `tests` has no seeder and no branch), and
    # nothing reads this key to pick or drop branches — it exists so the value
    # an operator believed they set is on the record. A previous design DID
    # consume a count (defaulted to 3 against six families, never recording
    # which three ran), which is exactly why the branch list is now the one
    # authority.
    surveyPasses: 6

    # How many of those families run AT ONCE (WP11c).
    #
    # A CEILING the run clamps again per backend, never the effective value. On
    # the shipped default backend (gondolin) each agent session is a QEMU
    # micro-VM inside the harness process, so it pins to 1 and this key changes
    # nothing at all until that is measured; `none` and `docker` take it as
    # written.
    #
    # Six because the six families write six disjoint, append-only
    # `hypotheses/<family>.jsonl` and never read each other's — there was never
    # an ordering constraint between them, only a scheduler that ran one DAG
    # node at a time. Chained, they were 851s of a 29-minute review (49% of the
    # wall clock); concurrent, they cost the slowest single family.
    #
    # Lower it to bound provider rate-limit pressure or memory, NOT to bound
    # spend: six passes cost the same in tokens either way.
    surveyConcurrency: 6

    # ── WP4: `prepare` (the affordance) + `falsify` (the oracle) ──────────────
    #
    # A SECOND switch under an already-gated block, and TRI-STATE — the middle
    # value is the point of it.
    #
    #   "off"    — neither `prepare` nor `falsify` runs. Reproduces WP3 exactly,
    #              and it is the shipped posture: the whole pipeline is off out
    #              of the box (LD8).
    #   "static" — both phases run and NOTHING IS EVER INSTALLED. `prepare` runs
    #              with `--no-install`, so it writes a real `env.json`
    #              (`install: "skipped"`, `installed` still read off the
    #              filesystem) and `falsify` gets the fact its prompt is written
    #              against: it already branches on `installed: false`, records
    #              what it cannot run as `unprobed` with a reason, and those
    #              hypotheses SURVIVE to adjudication. The oracle was written
    #              for the zero-install world; one shared gate was all that kept
    #              it out of one. Seconds of CPU, no package manager, no test
    #              suite, no `postinstall` from a pull request head.
    #   "full"   — `prepare` installs. What that buys is the DISCOVERY side, not
    #              the probe side: a tsconfig that `extends` a bare package
    #              specifier (`@calcom/tsconfig/react-library`) does not resolve
    #              on a bare checkout, tsgo excludes the project, and the case
    #              drops to tier 2 with `contracts` emitting nothing — measured
    #              over the 50-PR corpus, tier-1 cases 21 -> 5 and contract
    #              deltas 73 -> 19. It is also the only route to a coverage
    #              artifact, so until `probeCoverage` produces one the `tests`
    #              family is NOT MEASURED, a different row from "did not
    #              convert".
    #
    #              Two costs that are NOT metered anywhere, and this is the only
    #              place they are written down. There is no disk guard — not in
    #              `prepare`, not in `prepareTree`, not in the workflow — and
    #              the warm-workspace refresh is deliberately
    #              `git clean -fdx -e node_modules`, so `node_modules` PERSISTS
    #              across reviews by design. `full` is therefore a permanent
    #              per-target disk cost with nothing watching the volume. And
    #              the `facts` phase inherits the installed tree on every
    #              re-review; peak RSS there is UNMEASURED on the current
    #              engine.
    #
    # A bare `true` reads as "static", never "full": no deployment may silently
    # gain an install by upgrading. Anything else — including the truthy strings
    # — reads as "off", the same direction every switch in this block fails.
    probes: "off"
    # Let the install run the tree's own postinstall. Applies to `full` alone —
    # there is no install to attach a script to under `static`, and the phase
    # never claims otherwise in `env.json`. OFF, as a SECURITY default:
    # `prepare` installs at a PULL REQUEST HEAD, so a lifecycle script there is
    # code the PR author wrote running on this machine — and `pr-review`'s
    # workspace has never installed anything, which makes this the first thing in
    # the workflow that could. Making an `extends` resolve needs the files, not
    # their scripts.
    probeLifecycleScripts: false
    # The repo's own `tsc --noEmit`, for per-file/per-line diagnostics a
    # hypothesis can be anchored to. NOT a CI re-run: CI already reports
    # pass/fail over a matrix and `{{ciSection}}` is already in the context.
    probeTypecheck: false
    # The ONE step in this pipeline that runs a test suite — the wall-clock item
    # the design review deleted along with `mutants`. Its own switch because it
    # is its own price: everything else in `prepare` is seconds, this is minutes.
    # It never guesses a command, only one the repo itself named (a `coverage` /
    # `test:coverage` script).
    probeCoverage: false
    prepareTimeoutSeconds: 300
    coverageTimeoutSeconds: 900
    # Phase budgets for the pipeline's deterministic steps in pr-review.yaml
    # (`facts`, `seed`, `reconcile`), read as `{ from: … }` — no literal in YAML.
    factsTimeoutSeconds: 300
    seedTimeoutSeconds: 120
    reconcileTimeoutSeconds: 120
    # Rounds `falsify` gets to write and run probes. Two: the loop's exit is a
    # five-line existence gate, not a validator — v2's full quote validator was
    # overkill and cost 2.4x for a worse result.
    probeRounds: 2
    # The WHOLE-PHASE ceiling on `falsify`, across every round. It had none at
    # all until the oracle could run without an install, and `probeRounds` was
    # never a budget: a round count bounds how many times the loop may go round,
    # not what it may spend, and CPU is this pipeline's binding constraint. Ten
    # minutes covers two rounds of a pass whose probes are sub-second by
    # construction (a differential `git` run, a copied pure function under plain
    # `node`) and whose real cost is model latency.
    falsifyTimeoutSeconds: 600
    # The WHOLE-PHASE ceiling on `jev-classify`, under `adjudicate: "jev"`.
    # Each call is a TypeSafe `systemOne` round trip (~100ms typically) but the
    # phase makes one per hypothesis and a case can carry dozens; 120s covers
    # a slow provider day without threatening the falsify-sized budget above.
    jevTimeoutSeconds: 120
    # The attention boundary (WP6b). Preserving internal recall and spending a
    # human's attention are two different budgets; conflating them is how a
    # recall-first reviewer becomes unreadable.
    #
    # Everything past the inline budget goes to the review BODY under
    # "Additional findings" — still posted, still visible. Nothing is dropped
    # for being noisy: this is an attention boundary, not the v2 suppressor that
    # was built, measured and reverted.
    #
    # Five, down from ten (issue #405). Ten came from "Does AI Code Review Lead
    # to Code Changes?" (22k+ real review comments; the wall it warns about is
    # twenty) and never bound while inline volume was 1–5 per PR. The all-open
    # Martian arm then posted 5–13 a case at precision 0.15–0.40 against two
    # gold. It can bite now because what it ranks on varies: a hypothesis-
    # derived finding's severity is DERIVED from its evidence record and probe
    # strength (code-facts finding-severity.ts, stamped by `reconcile`), so the
    # cut keeps boundary-crossing and executed claims over local, read-only
    # ones. The overflow still goes to the body, then to the body budget.
    maxInlineComments: 5
    # The BODY budget — how many findings the review body's "Additional
    # findings" list may carry. N (the default is 5) keeps the top N ranked by
    # severity and records the rest in disposition.json at
    # `internal` with reason `body-budget` — capped attention, never a lost
    # record. 0 = NO overflow at all (nothing tiers to body); null = unlimited
    # (the pre-2026-08-24 funnel).
    #
    # Why 5 and not the 0 this shipped with: 0 measured better on the external
    # set under the production SONNET-adjudicator shape (29 of 36 matched gold
    # kept, precision 0.263 → 0.492, F1 0.362 → 0.479, above the two-phase
    # baseline) — but the same sweep showed that win is adjudicator-shape-
    # conditional. With a weak adjudicator the body tier carries most of the
    # recall (Haiku-everywhere: posted recall 0.42 → 0.12 at cap 0), while mid
    # caps kept nearly all of it (skillspro cap 4: 0.300, cap 8: 0.380 vs 0.420
    # unlimited; Martian cap 4: 0.548, cap 8: 0.581 vs 0.581 unlimited at
    # slightly better precision). 5 is the recall-preserving compromise pending
    # a real boundary tune — so an eval overlay measuring a particular shape
    # should still pin this key (`null` for the open funnel) explicitly rather
    # than inherit it. A deployment overlay should NOT pin `null`: that is the
    # open funnel issue #405 measured posting every claim, and the body budget
    # is only a ranking cut once severity varies (see maxInlineComments).
    maxBodyComments: 5
    # REMOVED 2026-09-21: `thresholds` (the per-family confidence bars) and
    # `internalFloor`. Both gated on a finding's self-reported `confidence`,
    # which measured AUROC 0.228 [0.171, 0.299] over 516 findings from 20
    # preserved case-runs — a strong signal pointing the WRONG way, because the
    # model is surest about the observations where nothing is wrong (withheld
    # findings carried a HIGHER median confidence, 0.99, than posted ones).
    # They also cost nothing: across the preserved archive not one gold finding
    # was ever lost to `below-floor` or `below-threshold`. An overlay that
    # still pins either key is accepted and ignored, with a warning in the log.

# The software-factory pipeline (label-driven autonomy).
#
# An issue labelled `ready-for-agent` on a repo listed below dispatches a gated
# `build` run: the harness moves the label on, runs the workflow behind its
# human approval gates, and parks the result on a label a human reads.
#
# IT SHIPS INERT. `repos: []` means no issue label routes anywhere — the router
# reads this block on every `issue.labeled` event, and with no repo listed the
# label is not a pipeline stage at all, so the event is dropped where it
# arrives. A deployment that says nothing about `autonomy:` behaves exactly as
# it did before the block existed.
#
# OPERATOR-ONLY, deliberately — the same reasoning `review.analysis` carries
# above. Autonomy is SPEND on the operator's budget, and there is no "more
# conservative" direction for a repo to clamp it toward, so there is no
# repo-settable autonomy key and `repoConfig.allowKeys` must NOT gain one: a
# repo that could add itself here could spend the operator's money by committing
# a file. A repo opts OUT with the affordances it already owns — an explicit
# `disabled.workflows: [build]` in its `.lastlight/lastlight.yml` turns the
# pipeline off for the whole repo, and the hold label turns it off for one
# issue.
autonomy:
  # Exact `owner/repo` names. NO GLOBS, by design: enabling an org by pattern is
  # how a repo nobody reviewed acquires an agent — and a budget — the moment
  # somebody creates it. Matching is exact string equality
  # (`isAutonomousRepo()`); `owner/*` is a repo name that will never exist.
  repos: []
  # The stages a label can start. `build` is the one shipped stage.
  stages:
    build:
      # The label a human — or the triage agent — applies to START the stage. A
      # stage with no `enter` label can never fire, so config load DROPS one
      # rather than keeping it: a stage that is present in the `/config` view
      # and unreachable in fact is a silent trap.
      enter: ready-for-agent
      # What the harness advances the label to AT DISPATCH — before the run has
      # started, not after it finishes. That ordering is the whole point: while
      # `enter` is still on the issue, every backstop sweep and every re-
      # delivered webhook sees a pipeline entry it is entitled to pick up, and
      # dispatches the same build again. Moving the label first makes the issue
      # its own lock.
      running: agent-building
      on_success: ready-for-human  # terminal — a human reviews, and merges, the PR
      on_failure: agent-blocked    # terminal — the run stopped; a human reads why
      workflow: build
      # Human-in-the-loop approval gates, ON by default and merged ADD-ONLY onto
      # the run's effective approval map: this block can turn a gate ON that the
      # workflow left off, never off one the operator's `approval:` block asked
      # for. The names are the gates `workflows/build.yaml` already declares.
      gates:
        post_architect: true
        post_reviewer: true
      # What becomes of the pull request the build opens:
      #   none             the PR parks at `on_success` and a human merges it —
      #                    the default, and the only value that ships on
      #   auto             the build agent enables GitHub AUTO-MERGE, so CI and
      #                    branch protection still gate the merge; the agent
      #                    never presses the button itself
      #   auto-low-impact  NOT IMPLEMENTED YET, and it is worth saying plainly:
      #                    there is no impact signal for a FEATURE PR today
      #                    (`dependencies.autoMergeMaxImpact` scores dependency
      #                    bumps and nothing else). The value is accepted and
      #                    recorded, and behaves as `none` until one exists.
      on_merge: none
  # Bounds on the pipeline as a whole, enforced at the DISPATCH GATE — before a
  # run starts, never mid-flight — and EVERY refusal is recorded, so "a quiet
  # day" and "the budget said no" are distinguishable after the fact. They bound
  # the one real failure mode: a label applied in bulk by a triage sweep, a
  # re-import or another bot, turning into N concurrent agent runs.
  budget:
    maxConcurrentBuilds: 2      # across every repo, at any instant
    maxBuildsPerRepoPerDay: 3   # per repo, rolling day
    dailyUsd: 25                # deployment-wide model spend per day
    repoDailyUsd: 10            # one repo's share of it

# Retry policy for every PR_FIX_SHAPED workflow — pr-fix and dependabot-ci-fix
# (issue #251). The loop diagnoses a CI failure before it retries, and these are
# the budgets that bound it.
#
# Repo-settable (a repo may commit its own values in `.lastlight/lastlight.yml`),
# clamped ONE WAY — a repo can only ever be MORE conservative than this block:
#   maxAttempts / localIterations / maxFlakyDeferrals   min(repo, operator)
#   maxCostUsd                                          min, `null` = unbounded
#   retryableClasses                                    a subset of this list
# `escalateModelAfterAttempt` (spend) is OPERATOR-ONLY — a repo setting it is
# dropped with a warning. The gate's until_bash budget moved to
# `gate.timeoutSeconds` (above); `fix.gateTimeoutSeconds` is a deprecated alias.
fix:
  maxAttempts: 3               # cross-run attempts per (repo, PR) before requires-human
  localIterations: 2           # within-run gate-loop iterations inside ONE attempt
  escalateModelAfterAttempt: 1 # attempts above this use models["pr-fix-retry"] when set
  maxCostUsd: 5.0              # cumulative ceiling across attempts for one PR; null = off
  maxFlakyDeferrals: 2         # a `flaky` verdict defers this often, then counts as reproducible
  # Diagnosis classes another attempt can help with; every other class escalates
  # immediately instead of burning budget. The vocabulary is
  # reproducible | env-mismatch | flaky | infra-dependent | upstream-broken.
  retryableClasses: [reproducible, env-mismatch]

# How dependency PRs are merged (issue #252). Impact — not semver magnitude — is
# what gates a MAJOR bump: a @types/* major is not a framework rewrite.
#
# Repo-settable and clamped one way, as above:
#   autoMergeMaxImpact    the LOWER tier on none < low < medium < high
#   requireSettledChecks  add-only `true` (a `false` is dropped with a warning)
#   auditComment          free — cosmetic
# `minSettledChecks` is OPERATOR-ONLY: clamping it `max(repo, operator)` would
# weld the escape hatch shut for a repo with no CI at all.
dependencies:
  autoMergeMaxImpact: medium   # none | low | medium | high — ceiling for auto-merging a MAJOR
  requireSettledChecks: true   # enforce settled-"passing" on ALL routes (webhook, cron, comment)
  minSettledChecks: 1          # an auto-merge decision needs >= N settled checks; 0 = legacy behaviour
  auditComment: true           # post the evidence comment when auto-merging a major

# Which crons participate (issue #180). The SAME block is valid here, in your
# overlay's config.yaml, and in a managed repo's .lastlight/lastlight.yml — the
# meaning depends on the layer:
#
#   operator (here / overlay)  disable: [x]  → cron x is off globally
#                              enable:  [x]  → no-op; a cron is on by default
#   repo (.lastlight/)         disable: [x]  → THIS repo drops out of x's fan-out
#                              enable:  [x]  → THIS repo opts in even when x is
#                                              off globally
#
# A name in BOTH lists is disabled, at every layer — a cron that doesn't run is
# the safe reading of a contradictory config.
#
# `disabled.crons` below is the LEGACY spelling of `crons.disable` and still
# works exactly as before: it is unioned into this list (and additionally drops
# the cron's definition at asset-load time, so it is the harder kill).
#
# NOTE: `crons.disable` here means "off BY DEFAULT" — a repo listed in
# `repoConfig.allowKeys` can still opt itself back in. If you want a kill switch
# no repo can override, drop `crons` from repoConfig.allowKeys (below); that
# leaves this list as the only word on the subject.
crons:
  enable: []
  disable: []

disabled:
  workflows: []
  # Legacy alias for `crons.disable` above — kept working, not deprecated.
  crons: []
  prompts: []
  skills: []
  agentContext: []

routes:
  github:
    issue_opened: issue-triage
    issue_answer: answer
    issue_reopened: issue-triage
    # A pipeline STAGE label was applied to an issue (the software-factory
    # flow). The router drops every label that is not a configured
    # `autonomy.stages.*.enter`, so this is inert until `autonomy.repos` names
    # a repo — and it is guard 1 of the four that replace the bot-sender filter
    # we deliberately opened for `issues.labeled`.
    issue_labeled: build
    pr_opened: pr-review
    pr_synchronize: pr-review
    pr_reopened: pr-review
    # `review.trigger: after-checks` — the head SHA's checks have settled on a
    # PR that neither the fix nor the merge route claimed.
    pr_checks_settled: pr-review
    # `review.requestLabel` was applied (the `on-request` mechanism).
    pr_labeled: pr-review
    # A review was requested from the bot by name, or the `last-light/review`
    # check's Re-run button was pressed.
    pr_review_requested: pr-review
    approval_response: approval-response
    security_review: security-review
    verify: verify
    qa_test: qa-test
    demo: demo
    pr_fix: pr-fix
    pr_review: pr-review
    pr_comment: pr-comment
    issue_build: build
    issue_explore: explore
    issue_comment: issue-comment
    security_feedback: security-feedback
    explore_reply: explore-reply
  slack:
    reset: chat-reset
    status: status-report
    approve: approval-response
    reject: approval-response
    build: build
    triage: issue-triage
    review: pr-review
    security: security-review
    verify: verify
    qa_test: qa-test
    demo: demo
    explore: explore
    answer: answer
    chat: chat
    explore_reply: explore-reply

cron:
  webhooksEnabledCondition: true

# Where a repo's outbound notifications go. TODAY this is read only by the
# weekly Slack digest (`workflows/cron-digest.yaml`).
#
# This block is the BASE the per-repository layer merges onto — a managed repo
# sets `notifications: {slack: {channel: C01ABCDEFGH}}` in its
# `.lastlight/lastlight.yml` and its digest goes there. Leaving it `null` here
# is the point: an operator routes repos with the `slack.repoChannels` map
# below, which is per-repo by construction, whereas this block is not.
#
# `channel: null` committed by a REPO is not the same as absent — it means "send
# me no digest" and beats the operator's map. Absent means "I have no opinion,
# use whatever you've configured".
notifications:
  slack:
    channel: null

slack:
  # Operator-side channel routing: "owner/repo" -> Slack channel id. Consulted
  # after the repo's own `notifications.slack.channel` and before the global
  # SLACK_DELIVERY_CHANNEL fallback. Set it in the instance overlay's
  # config.yaml, not here.
  #
  #   repoChannels:
  #     nearform/lastlight: C01ENGINEERING
  #
  # Channel ids are not secrets, so they show up unredacted on /admin/api/config.
  repoChannels: {}

# The weekly repo digest — what happened in a repo, and what Last Light did
# about it, posted to that repo's Slack channel.
#
# It is INERT until a channel resolves for a repo (see `notifications` above):
# no channel, no post, no cost. That is deliberate — a fresh install should not
# start talking to a workspace nobody configured.
#
# The facts are computed in code, from the GitHub API and the harness's own
# SQLite, so the numbers are right. `narrative` adds ONE cheap model call that
# turns the week's merged PRs and issues into a few sentences of plain English;
# it is never asked to produce a number, and a failure there drops the summary
# rather than the digest.
#
# The content lists (which PRs merged, which issues opened and closed) come from
# a second, GraphQL request that is likewise allowed to fail: if it does, the
# digest posts the counts alone, exactly as it did before the lists existed.
digest:
  windowDays: 7        # how far back a digest looks
  narrative: true      # false = counts + lists only, zero model spend
  maxItems: 5          # cap on the escalation list
  listItems: 8         # cap on each content list (merged PRs, issues opened/closed); max 25
  detailItems: 25      # how many items' text the summariser reads; max 60 — it sizes a prompt
