---
import DocsLayout from '../../layouts/DocsLayout.astro';
---
<DocsLayout
	slug="local-dev"
	description="Clone, configure, and run Last Light on your dev machine — agents still run in real per-phase sandboxes."
>
	<p>
		Local dev runs the harness on your host (so you get hot reload) but spawns every
		agent task in a real per-phase sandbox, exactly like production. This is deliberate:
		it's the only honest way to develop against the bot's actual execution model.
	</p>

	<p>
		The default sandbox backend is <strong>gondolin</strong> — agentic-pi's QEMU
		micro-VM, using HVF on macOS or KVM on Linux. No Docker needed. Switch to
		<code>LASTLIGHT_SANDBOX=docker</code> if you'd rather run sibling Docker containers
		(useful for prod-like smoke testing), or <code>none</code> for in-process dev runs.
	</p>

	<h2>1. Clone and install</h2>

	<pre><code>git clone https://github.com/nearform/lastlight.git
cd lastlight
npm install</code></pre>

	<h2>2. Create <code>.env</code></h2>

	<pre><code>cp .env.example .env</code></pre>

	<p>Edit <code>.env</code> and fill in the four values you collected in the previous step:</p>

	<pre><code>GITHUB_APP_ID=123456
GITHUB_APP_PRIVATE_KEY_PATH=./your-app.private-key.pem
WEBHOOK_SECRET=your-webhook-secret</code></pre>

	<p>
		Drop the <code>.pem</code> file into the project root (or wherever
		<code>GITHUB_APP_PRIVATE_KEY_PATH</code> points). It's already in
		<code>.gitignore</code>, so you won't accidentally commit it.
	</p>

	<h2>3. (Docker mode only) Build the sandbox image</h2>

	<pre><code>docker compose --profile build-only build sandbox</code></pre>

	<p>
		Only needed if you set <code>LASTLIGHT_SANDBOX=docker</code>. The default gondolin
		sandbox runs from the harness directly and needs no image build. Rebuild when
		<code>sandbox.Dockerfile</code> changes.
	</p>

	<h2>4. Set a model API key</h2>

	<p>
		The agent runtime is <a href="https://github.com/nearform/lastlight/tree/main/packages/agentic-pi" target="_blank" rel="noopener">agentic-pi</a> —
		it runs on any model the <a href="https://github.com/earendil-works/pi" target="_blank" rel="noopener">pi coding agent</a> supports
		(Claude, GPT, Gemini, Llama, and more). Drop the matching key into <code>.env</code>:
	</p>

	<pre><code># Default model is anthropic/claude-sonnet-4-6
ANTHROPIC_API_KEY=sk-ant-...

# Or, if you'd rather route to OpenAI
OPENAI_API_KEY=sk-...
LASTLIGHT_MODEL=openai/gpt-5.5

# Or use OpenRouter — one key, many models
OPENROUTER_API_KEY=sk-or-v1-...
LASTLIGHT_MODEL=openrouter/anthropic/claude-sonnet-4.5

# Sandbox backend — gondolin (default), docker, or none
# LASTLIGHT_SANDBOX=gondolin</code></pre>

	<p>
		No interactive login step. pi-ai reads the API key from the harness env and
		forwards it into each sandbox.
	</p>

	<h2>5. Start the harness</h2>

	<pre><code># Server + dashboard, with hot reload
npm run dev

# Or individually
npm run dev:server
npm run dev:dashboard</code></pre>

	<p>Under the hood, <code>npm run dev:server</code> calls <code>scripts/dev-local.sh</code>, which:</p>

	<ul>
		<li>When <code>LASTLIGHT_SANDBOX=docker</code>, verifies Docker is running and the sandbox image exists.</li>
		<li>Creates <code>./data/agent-sessions/</code> and <code>./data/secrets/</code>, and bind-mounts <code>./data</code> at <code>/data</code> in docker-mode sandboxes, as the production volume is. A sandbox reads the GitHub App key from <code>/data/secrets/app.pem</code> and the OAuth logins from <code>/data/auth.json</code>.</li>
		<li>
			Sets safe defaults: <code>LASTLIGHT_LOCAL_DEV=1</code>,
			<code>STATE_DIR=./data</code>, <code>LASTLIGHT_SESSIONS_DIR=./data/agent-sessions</code>.
		</li>
		<li>Launches the harness with <code>tsx watch src/index.ts</code>.</li>
	</ul>

	<div class="docs-callout tip">
		<div class="docs-callout-body">
			<code>LASTLIGHT_LOCAL_DEV=1</code> tells the harness <strong>not</strong> to
			touch your personal <code>~/.gitconfig</code> or keychain. Everything
			sandbox-related stays inside <code>./data/</code>. Safe to run on a dev laptop.
		</div>
	</div>

	<h3>Developing against Postgres</h3>

	<p>
		Dev uses SQLite by default, like production. To develop against the
		Postgres runtime instead, the compose file ships an opt-in
		<code>postgres</code> profile — a <code>postgres:16</code> container bound
		to <strong>127.0.0.1:55432</strong> (a high port so it cannot collide with
		a Postgres you already run, and loopback-only because the default
		credentials are dev credentials):
	</p>

	<pre><code>npm run dev:db:up          # start it
npm run dev:db:migrate     # copy ./data/lastlight.db into it (one way, verified)
LASTLIGHT_DEV_DB=postgres npm run dev

# or persist the choice in apps/server/.env:
#   LASTLIGHT_DEV_DB=postgres
npm run dev:db:psql        # a psql shell in the container
npm run dev:db:down        # stop it (the volume survives)</code></pre>

	<p>
		<code>dev-local.sh</code> fills in the URL for you and refuses to start if
		nothing is listening on the port — otherwise the failure surfaces as a
		wrapped migrator error several lines deep. An explicit
		<code>DATABASE_URL</code> always wins, and
		<code>LASTLIGHT_DEV_DB=sqlite</code> goes back to the file.
	</p>

	<p>
		The same profile is a reasonable way for a self-hoster to run Postgres
		beside the stack (<code>docker compose --profile postgres up -d postgres</code>,
		then <code>DATABASE_URL=postgres://…@postgres:5432/lastlight</code> in
		<code>instance/secrets/.env</code>) — but set <code>POSTGRES_PASSWORD</code>
		if you do. It is inert unless the profile is named, so a normal
		<code>docker compose up</code> never starts it.
	</p>

	<h2>6. Trigger and debug via the CLI</h2>

	<p>
		The <code>lastlight</code> CLI is a thin HTTP client — it POSTs triggers and reads
		the instance's admin API. In dev use <code>npm run cli -- &lt;args&gt;</code> (or
		<code>npx tsx src/cli.ts &lt;args&gt;</code>); installed globally it's just
		<code>lastlight</code>. Open a second terminal:
	</p>

	<pre><code># Authenticate once — saves the instance URL + token to ~/.lastlight
lastlight login http://localhost:8644          # browser handoff (password / Slack / GitHub)
lastlight login http://localhost:8644 --password   # headless fallback
lastlight status                               # instance, server health, token validity

# Cheap defaults — single agent invocation
lastlight &lt;github-url&gt;                          # default: triage the issue
lastlight owner/repo#N                          # shorthand: triage the issue
lastlight https://github.com/owner/repo/pull/99 # review one PR
lastlight triage owner/repo                     # scan repo for new issues
lastlight review owner/repo                     # scan repo for PRs to review
lastlight health owner/repo                     # weekly-style health report

# Expensive, opt-in — full Architect → Executor → Reviewer → PR cycle
lastlight build owner/repo#42

# Debug a running instance instead of SSH (all accept --json)
lastlight workflow list --status failed         # recent failed runs
lastlight workflow log &lt;run-id&gt; --follow         # phases + live tail
lastlight session list                          # recent sandbox sessions
lastlight session log &lt;session-id&gt; --follow     # live transcript (SSE)
lastlight logs search "ECONNREFUSED"            # search errors / transcripts
lastlight approvals list                        # pending approval gates
lastlight cron list                             # scheduled jobs + next/last run
lastlight cron trigger &lt;name&gt;                    # fire a cron now (testing)
lastlight pr retry owner/repo#42 "flaky runner"  # un-stick a PR the bot gave up on
lastlight stats --daily 14                      # cost / token rollups</code></pre>

	<p>
		The default action for a single-issue or single-PR shorthand is the cheap one
		(triage / review). Build cycles are always opt-in via the explicit <code>build</code>
		subcommand. The debug commands read the same data the dashboard shows, so you can
		investigate a remote instance from your terminal without SSHing in. See the
		<a href="/docs/cli">CLI reference</a> for the full command list and global flags.
	</p>

	<h2>7. Open the dashboard</h2>

	<p>
		The admin dashboard is served at <a href="http://localhost:8644/admin" target="_blank" rel="noopener">http://localhost:8644/admin</a>.
		The left rail has thirteen tabs; the ones to start with are <strong>Home</strong>,
		<strong>Board</strong> (the pipeline board — drag a card between columns to move an
		issue's stage, and dropping one on a build column starts a build),
		<strong>Workflows</strong>,
		<strong>Sandbox Sessions</strong>, and <strong>Chat Sessions</strong>. You can
		watch sessions stream live, drill into any run, and resolve approval gates.
	</p>

	<p>
		If you set <code>ADMIN_PASSWORD</code> in <code>.env</code>, the dashboard will
		prompt for it. Otherwise it's open.
	</p>

	<h2>Optional: receive real webhooks</h2>

	<p>
		To receive real GitHub webhooks in local dev, expose port <code>8644</code> with
		<code>ngrok</code> or <code>cloudflared</code> and point your GitHub App's webhook
		URL at the public tunnel. Without webhooks you can still trigger every workflow
		via the CLI.
	</p>

	<div class="docs-next">
		<a href="/docs/github-app">
			<span class="label">← Previous</span>
			<span class="title">Create a GitHub App</span>
		</a>
		<a href="/docs/production" class="next">
			<span class="label">Next →</span>
			<span class="title">Production deploy</span>
		</a>
	</div>
</DocsLayout>
