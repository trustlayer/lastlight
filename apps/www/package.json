{
  "name": "lastlight-www",
  "type": "module",
  "version": "0.0.2",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prepare": "node scripts/sync-spec.mjs",
    "predev": "node scripts/sync-spec.mjs && node scripts/sync-releases.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/sync-spec.mjs && node scripts/sync-releases.mjs",
    "build": "astro build && node scripts/generate-md.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy",
    "sync-spec": "node scripts/sync-spec.mjs",
    "sync-releases": "node scripts/sync-releases.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "cheerio": "^1.2.0",
    "node-html-markdown": "^2.0.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.123.0",
    "zod": "^4.3.6"
  }
}
